(rule
 (mode promote)
  ; This is generating a single SLR version of the core
 (action (with-stdout-to krnl_msm_pippenger.v (run ../../hardcaml/bin/generate.exe kernel -scalar-bits 253))))

(rule (target stub.sv) (action (write-file %{target} "")))
